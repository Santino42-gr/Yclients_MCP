# YClients MCP Server

MCP (Model Context Protocol) сервер для автоматизации записи клиентов в салон красоты через YClients API.

## Описание

Этот сервер предоставляет высокоуровневые инструменты для работы с YClients API, позволяя ИИ-ассистентам:

- 📋 Записывать клиентов на услуги
- 🔍 Искать доступное время
- 👤 Управлять клиентами
- ❌ Отменять записи
- 📊 Получать информацию об услугах и мастерах

## Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

Обязательные переменные:
- `YCLIENTS_BEARER_TOKEN` - Bearer токен для доступа к YClients API
- `YCLIENTS_COMPANY_ID` - ID вашей компании в YClients

### 3. Сборка

```bash
npm run build
```

### 4. Запуск

```bash
npm start
```

Или для разработки:

```bash
npm run dev
```

## Конфигурация

### Переменные окружения

| Переменная | Обязательная | По умолчанию | Описание |
|------------|-------------|--------------|----------|
| `YCLIENTS_BASE_URL` | Нет | `https://api.yclients.com/api/v1` | Базовый URL YClients API |
| `YCLIENTS_BEARER_TOKEN` | **Да** | - | Bearer токен для авторизации |
| `YCLIENTS_COMPANY_ID` | **Да** | - | ID компании в YClients |
| `MCP_SERVER_NAME` | Нет | `yclients-booking` | Имя MCP сервера |
| `MCP_SERVER_VERSION` | Нет | `1.0.0` | Версия MCP сервера |
| `LOG_LEVEL` | Нет | `info` | Уровень логирования |
| `RATE_LIMIT_REQUESTS_PER_MINUTE` | Нет | `60` | Лимит запросов в минуту |

### Получение Bearer токена

1. Зайдите в [личный кабинет YClients](https://yclients.com/)
2. Перейдите в раздел "API"
3. Создайте новый токен с необходимыми правами
4. Скопируйте Bearer токен в переменную `YCLIENTS_BEARER_TOKEN`

## Доступные инструменты

### 📋 Запись клиентов

#### `book_appointment`
Записать клиента на услугу. Автоматически найдет или создаст клиента, найдет услугу по названию, проверит доступность времени.

**Параметры:**
- `client_phone` (обязательно) - Телефон клиента в формате +7XXXXXXXXXX
- `client_name` (обязательно) - Имя клиента
- `service_name` (обязательно) - Название услуги
- `preferred_date` (обязательно) - Дата в формате YYYY-MM-DD
- `preferred_time` (обязательно) - Время в формате HH:MM
- `master_name` (опционально) - Имя мастера
- `comment` (опционально) - Комментарий к записи

#### `find_available_time`
Найти свободное время для записи на услугу.

**Параметры:**
- `service_name` (обязательно) - Название услуги
- `date_from` (обязательно) - Начальная дата поиска
- `date_to` (обязательно) - Конечная дата поиска
- `master_name` (опционально) - Имя мастера

### 👤 Управление клиентами

#### `find_client`
Найти клиента по телефону.

#### `get_client_bookings`
Получить записи клиента по телефону.

#### `cancel_booking`
Отменить запись клиента.

### 📊 Информация о салоне

#### `get_services_list`
Получить список всех доступных услуг салона.

#### `get_staff_list`
Получить список всех мастеров салона.

## Примеры использования

### Запись клиента

```json
{
  "name": "book_appointment",
  "arguments": {
    "client_phone": "+79123456789",
    "client_name": "Анна Иванова",
    "service_name": "стрижка",
    "preferred_date": "2024-12-25",
    "preferred_time": "15:00",
    "master_name": "Елена",
    "comment": "Первое посещение"
  }
}
```

### Поиск доступного времени

```json
{
  "name": "find_available_time",
  "arguments": {
    "service_name": "маникюр",
    "date_from": "2024-12-20",
    "date_to": "2024-12-22",
    "master_name": "Мария"
  }
}
```

## Архитектура

```
src/
├── index.ts              # Главный файл MCP сервера
├── yclients-client.ts    # YClients API клиент
├── types.ts              # TypeScript типы
├── tools/                # MCP инструменты
│   ├── booking.ts        # Инструменты для записи
│   ├── clients.ts        # Работа с клиентами
│   └── services.ts       # Услуги и мастера
└── utils/
    ├── validation.ts     # Валидация данных
    └── errors.ts         # Обработка ошибок
```

## Разработка

### Требования

- Node.js >= 18.0.0
- TypeScript >= 5.0.0

### Команды

```bash
# Разработка с автоперезагрузкой
npm run dev

# Сборка
npm run build

# Запуск продакшен
npm start

# Проверка типов
npm run type-check

# Линтер
npm run lint
```

### Добавление нового инструмента

1. Создайте метод в соответствующем классе инструментов
2. Добавьте описание инструмента в метод `getTool()`
3. Добавьте обработчик в метод `handle*()`
4. Зарегистрируйте инструмент в `src/index.ts`

## API YClients

Этот сервер работает с [YClients API v2](https://developers.yclients.com/ru/).

### Основные endpoints

- `/company/{id}/services/` - Список услуг
- `/company/{id}/staff/` - Список мастеров
- `/company/{id}/clients/` - Работа с клиентами
- `/company/{id}/book/` - Создание записи
- `/company/{id}/records/` - Управление записями
- `/company/{id}/staff/{id}/schedule/{date}` - Расписание мастера

### Rate Limits

YClients API имеет ограничения на количество запросов. По умолчанию сервер ограничивает до 60 запросов в минуту.

## Безопасность

- Никогда не коммитьте Bearer токены в репозиторий
- Используйте переменные окружения для конфиденциальных данных
- Регулярно обновляйте токены доступа
- Мониторьте логи на предмет подозрительной активности

## Лицензия

MIT

## Поддержка

При возникновении проблем:

1. Проверьте логи сервера
2. Убедитесь, что все переменные окружения настроены корректно
3. Проверьте доступность YClients API
4. Проверьте права доступа Bearer токена 